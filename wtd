- to choropleth 
- make a df with
-- array of coordinates
-- gridcode
-- elevation value

FINISHING
- mapstyle wip 
- value color v
- 2d or 3d v
- wide or dark
- per province wip
- cta on the map tip

WHAT WE HAVE
- Polygon irrigation every province with province centroid (lat, long) done
- Value irrigation (Newest Applied) done
- Combine All -> GEOJSON File every province -> Saved on irri_province folder done
- Valditing reducted geoms province vs actual geoms per province done

MAP FOR PROVISION THE D.I
- MarkerCluster (nama, from gridclass compute the action) done
## the purpose of markercluster per province merely a useless method to show all point in the narrower polygon (if there's just 2 markers in 1 province or less then its overkilling the dashboard)

TO PDK
(function)
- gpd.read GEOJSON file selected done
- gdf -> json interface done
- df empty diisi done
-- 'coordinates'
-- 'gridclass'
-- 'elevation' -> gridclass * C
-- 'colors' -> Map(gridclass)
- view_state -> centroid(lat, long) 
- polygon layer = pdk.layer(default) done
- deck -> style and map provider done
- add tooltip function ((sudah baik),(perlu opor), etc) done

Streamliting : 
- each province selector done
- each province map : 
-- choropleth pydeck done
-- markercluster
- Tooltip choropleth (max, min, mean, median)
- Legenda (SIA) -> choropleth
- Tooltip markercluster (Nama DI, Provinsi, Luas Ha)

